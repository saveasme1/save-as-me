/**
 * Cesium world clouds disabled — windshield uses Three.js photo cloud layers
 * from the early cockpit build (parallax planes), not floating world blobs.
 */
export function seedFlightClouds() {
  return null;
}
