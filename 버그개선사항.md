# SaveAs For Biz — 버그 개선사항

최종 반영: `main` `eacc0b6` (2026-08-21) · 사이트 https://save-as.me

## 핵심 원칙 (현재 방식 — 유지)

프로젝트 화면은 **공중 플레인/데칼 추정 좌표가 아니라**, A320 콕핏 GLB의 **검정 LCD 메시**에만 붙인다.

| 항목 | 값 |
|------|-----|
| 메시 | `111VUmesh_11` |
| 머티리얼명 | `black` |
| 배치 | 화면 NDC로 검정 면만 레이캐스트 → 로컬 자식 `PlaneGeometry` |
| 선택 팝 | 들어올림(lift) 금지 · 스케일만 미세 변경 |
| 글자 방향 | `CanvasTexture.rotation = -π/2` (메시 회전 대신 텍스처만) |

**하지 말 것:** `DecalGeometry`(메인 스레드 정지), 회색 패널(`cpit_5` / `111VUmesh_9`)에 폴백 부착, `lookAt(camera)` 빌보드, 큰 `pop` lift, 윈드실드에 투영되는 큰 구름 스프라이트.

---

## 이번 사이클에서 고친 것

### 1. MFD가 공중에 뜸 / 검정 화면이 비어 있음

- **원인:** DU 스크린 좌표(fy≈0.65)가 실제 검정 LCD보다 위라, 레이가 `black`이 아닌 패널 몸체(`111VUmesh_9`)에 맞음.
- **조치:**
  - 검정 메시만 `collectBlackMeshes`로 수집해 레이캐스트.
  - `measureBlackDuCenters`로 DU 벨트(대략 y 66–72%)의 x-갭 4개를 잘라 **실제 5개 DU** 중심·크기를 측정.
  - 실패 시에만 fallback 좌표(fy≈0.685) 사용.
  - 이전 스크린 dispose는 **새 배치 성공 확정 후**에만 수행(실패 시 기존 화면 복구).

### 2. 창에 흰 네모가 뜸

- **원인:** `softCloud` 스프라이트가 멀리 있어도 NDC상 윈드실드에 투영되어 흰 슬래브처럼 보임.
- **조치:** 소프트 클라우드 스프라이트 생성 비활성(`nCloud = 0`). 포토 클라우드 플레인은 더 멀리·낮은 opacity로만 유지.

### 3. 글자가 옆/거꾸로

- **원인:** 패널 로컬 축과 UV 방향 불일치. `rotateZ`로 메시를 돌리면 스케일/샘플이 깨짐.
- **조치:** 메시 자세는 `setFromUnitVectors(Z, nLocal)` 유지, **텍스처만** `tex.rotation = -Math.PI / 2`.

### 4. 비행/외부 (이전 커밋 포함, 유지)

- ↑ 기수 상승 / ↓ 하강 (Three.js +X 피치 부호 보정).
- 선회 soft-limit, 고도 바닥, 크루즈를 지형보다 멀리.
- 전방·측면 창이 같은 월드(지형/스카이)를 보도록 정렬.
- 비행 스크롤 방향(전진) 보정.

---

## 코드 위치

- `js/flight.js`
  - `measureBlackDuCenters` / `findBlackDuCenters`
  - `placeMfdOnBlackDus` / `installMfdScreens`
  - `paintMfdScreens` / `tickMfdPop`
  - `makeProjectPreview` (텍스처 회전)
  - 클라우드 스프라이트 블록 (`nCloud = 0`)

---

## 로컬 검증

```bash
npx --yes serve .
# http://127.0.0.1:4173 → 정면 뷰
```

확인 체크:

- [ ] 5개 검정 DU 안에 LOCAL / SAVEAS / MAKERBRIDGE / CURSOR MOBILE / PWA
- [ ] 좌측 창에 흰 사각 슬래브 없음
- [ ] 글자 가로·정방향
- [ ] 프로젝트 선택 시 화면이 캐빈 한가운데로 튀어나오지 않음

QA 스샷(로컬, 커밋 제외 가능): `assets/_qa-mfd-seated.jpg`, `assets/_qa-mfd-crop.jpg`

---

## 배포

```bash
git push origin main
```

GitHub Pages · `CNAME` → `save-as.me`. 반영 후 강력 새로고침(Ctrl+F5).

---

## 회귀 방지 메모

1. DU Y를 다시 올리면(`fy < ~0.68`) 패널 몸체에 붙어 **공중 부유가 재발**한다. 반드시 `black` 히트만 쓸 것.
2. `placeMfdOnBlackDus`가 실패했을 때 기존 메시를 먼저 dispose하면 화면이 전부 사라진다. 성공 후에만 교체.
3. 구름 스프라이트를 다시 넣을 거면, 윈드실드 NDC(대략 x 280–520, y 180–480)에 투영되는지 Playwright로 프로브할 것.

---

## 2026-08-22 — MFD 검정 DU 착좌 안정화 (runtime log 근거)

### 증상
- 새로고침 직후 프로젝트 MFD가 안 뜨고, 이륙(피치 변화) 즈음에야 뜨는 경우
- 뜨더라도 검정 LCD 베젤과 크기가 안 맞거나, 공중에 떠 보이거나, 카메라가 회색 벽에 박히는 회귀

### 로그로 확정한 원인
1. **카메라 `z = Math.max(1.25, …)` 강제**: `_camBase.z`가 ~0.38이어야 할 좌석 시점에서 1.25로 밀려 **벌크헤드/회색 면**만 보임. → **되돌림**: eacc0b6식 `center.z + size.z * 0.12`.
2. **gap-split 측정이 FO 쪽 island#4 중심을 두 DU 사이 틈(px≈1159)에 둠**: `fail_ray_miss` projectIndex 4 반복 → install 40회 실패. 피치가 `-0.02`가 아닐 때만 우연히 성공 → “이륙할 때쯤 뜸”.
3. **install 매 attempt마다 `frameSideScreens("center")`**: pitch thrash로 측정이 흔들림.
4. **세로 풀컬럼 레이 프로브**: DU 상단(py≈616)을 먼저 맞아 카드가 검정 **위**에 뜸.
5. **크기 캡 `0.14` / `0.195`**: 로그상 전 DU가 캡에 걸려 과대/과소.

### 적용한 수정 (`js/flight.js`, 배포 예: `?v=seedlock…`)
- **`measureBlackDuCenters`**: belt → modeY → eacc fx seeds(`0.339…0.656`) → 가로 스냅 후 **검정면 seed-grow** (gap-split 폐기).
- **`placeMfdOnBlackDus`**: 측정 중심 기준 **x+y 이웃 프로브**; 픽셀 기반 크기 `worldH * (wPx/w|hPx/h) * 0.96` (하드 캡 제거).
- **`state._mfdSeated`**: 5면 성공 후 잠금. look 버튼만 `force: true`로 재배치.
- **`installMfdScreens`**: 초반 `frameSideScreens` 호출 제거, 200ms부터 재시도.
- **`syncMfdCamera`**: rotation뿐 아니라 `_camBase` 위치/dolly도 맞춤 (측정 시 animate와 동일 시점).
- 디버그 ingest(`dbgMfd` / session `88eb62`)는 **이 문서 작성 시점에 코드에서 제거**.

### 건드리면 안 되는 것 (기존 규칙 유지)
- DecalGeometry / HTML clip overlay로 “공중 카드” 되돌리기
- 카메라 z를 다시 `Math.max(1.25,…)`로 올리지 말 것
- gap-split만으로 FO 5면을 나누려 하지 말 것 (틈 중심 재발)
- PowerShell `Set-Content` UTF-8로 `flight.js` 저장 (BOM/깨짐)

### 검증 포인트
- 하드 리프레시 직후(이륙 전) 5검정 DU 안에 LOCAL/SAVEAS/MAKERBRIDGE/CURSOR MOBILE/PWA
- 회색 벽/멈춤 없음, 조종석+하늘 정상
